# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  registry: "ghcr.io"
  repository: "io-err/kbot"
  tag: "v1.1.1-b9031f2"
  os: linux
  arch: amd64
secret:
  name: "telegram-bot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
